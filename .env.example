# Database — PostgreSQL (use docker-compose.yml to run locally)
DATABASE_URL="postgresql://gmate:gmate_dev@localhost:5433/gmate?schema=public"

# AI Companion (optional — omit AI_BASE_URL to disable AI features)
# AI_BASE_URL="http://localhost:8000/v1"
# AI_API_KEY="not-needed"
# AI_MODEL="default"

# Auth — generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"
